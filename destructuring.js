const profile ={
    fristName: "Raffi",
    lastName: "Ahmad",
    age: 30
}

const firstName = profile.fristName
const lastName = profile.lastName
const age = profile.age

console.log(firstName, lastName, age);

